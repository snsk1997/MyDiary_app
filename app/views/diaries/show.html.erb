<%= render "shared/header" %><br>

<%= link_to "Edit", edit_diary_path %><br>
<%= link_to "Delete", diary_path, method: :delete %><br>

<%= @diary.record_day %>
のDiary<br>
<%= @diary.weather.name %><br>
<%= @diary.title %><br>
<% @diary.images.each do |image| %>
    <%= image_tag image.variant(resize: '500x500') if @diary.images.attached? %><br>
  <% end %>
<%= @diary.content %><br>

<%= link_to "Back", user_path(current_user.id) %>

<%= render "shared/footer" %>