<%= render "shared/header" %><br>

<%= link_to "Edit", edit_diary_path %><br>
<%= link_to "Delete", diary_path, method: :delete %><br>

<%= @diary.record_day %>
のDiary<br>
<%= @diary.weather.name %><br>
<%= @diary.title %><br>
<%= image_tag @diary.image.variant(resize: '500x500') if @diary.image.attached? %><br>
<%= @diary.content %><br>

<%= link_to "Back", root_path %>

<%= render "shared/footer" %>