ようこそ、MyDiaryへ!!<br>

<% @diary.each do |diary| %>
  <%= link_to diary_path(diary.id) do %>
    <%= diary.title %><br>
  <% end %>
  <%= diary.content %><br>
  <%= diary.record_day %><br>
  <%= diary.weather.name %><br>
<% end %>

<%= link_to '日記を書く',new_diary_path, class: 'purchase-btn'%>

